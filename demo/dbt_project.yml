## Project
name: demo
version: "1.0"
config-version: 2
profile: demo

## Sources
model-paths: [models]
analysis-paths: [analysis]
test-paths: [tests]
seed-paths: [seeds]
macro-paths: [macros]
snapshot-paths: [snapshots]

## Target
target-path: target
clean-targets: [target, dbt_modules]

## Models
models:
  demo:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: edw